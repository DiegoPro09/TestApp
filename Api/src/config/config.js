//Se crea un modulo con la clave para ingresar a la api externa y la url de la misma
export const config = {
    secretKey: 'Bearer aSuperSecretKey',
    api: 'https://echo-serv.tbxnet.com/v1/secret'
}